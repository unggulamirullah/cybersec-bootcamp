# Lab Day 4: Intro to tcpdump

## Objektif
Menginstal dan menggunakan `tcpdump` di lingkungan Linux (WSL Ubuntu) untuk menangkap lalu lintas jaringan dasar dan menyimpannya ke dalam file `.pcap`.

## Langkah-Langkah Praktik

**1. Persiapan Environment**
Memperbarui *package list* dan menginstal `tcpdump`.
```bash
sudo apt update
sudo apt install tcpdump -y
